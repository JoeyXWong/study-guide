/**
 * Remove duplicates from an unsorted list, don't use temporary buffer
 * 
 * With buffer: create a set of ints and loop through every element, if hte element exists
 * point the previous node to current node.next, if the next node is also duplicate, keep moving until the next
 * node is not duplicate.
 * 
 */

const removeDups = () => {
    
}